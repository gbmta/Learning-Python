# What's the answer to life?
def exercise1() :
    answer = input('whats the answer to life?')
    if int(answer) == 42 :
        print ('Correct!')

# was having a lot of trouble with getting the if statement
# to recognize the answer as an int instead of a str. Finally figured it out.

# Another method of keeping an input as an int

def exercise2() :
    answer = input('whats the answer to life')
    int_answer = int(answer)

    if int_answer == 42 :
        print('correct')
        print(type(int_answer))
    
#I set it up this way so that I could practice "Def" function and so that I could test different exercises in the same module
exercise1()

